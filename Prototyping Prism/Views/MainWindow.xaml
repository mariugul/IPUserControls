<Window x:Class="Prototyping_Prism.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ip="clr-namespace:IPUserControls;assembly=IPUserControls"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="218.122" Width="443.999">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                <Label Content="IP Address" VerticalAlignment="Center"></Label>
                <ip:IpField></ip:IpField>
                <ip:IpPort></ip:IpPort>
                <ip:IpStatus></ip:IpStatus>
            </StackPanel>
            <Button Content="Connect" Width="100"></Button>
        </StackPanel>
    </Grid>
</Window>