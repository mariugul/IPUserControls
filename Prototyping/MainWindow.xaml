<Window x:Class="Prototyping.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ipUserControls="clr-namespace:IPUserControls;assembly=IPUserControls.Wpf"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="Test Window" Height="133.733" Width="395.733" >
    <Grid>
        <StackPanel Orientation="Horizontal">
            <Label Content="NCU" />
            <ipUserControls:IpField Name="IpField" VerticalAlignment="Top"/>
            <ipUserControls:IpPort  Name="IpPort" DefaultPortNumber="10" />
            <ipUserControls:IpStatus Name="IpStatus" />
            <Button Content="Enable/Disable Port" Height="20" VerticalAlignment="Top" Click="Button_Click" />
        </StackPanel>
    </Grid>
</Window>
